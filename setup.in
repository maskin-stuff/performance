#! /usr/bin/env false

MASKINEN_PATH=
PRINT_PATH=
POEM_PATH=

mkdir -p bin current chords grammars

ln -sf "$MASKINEN_PATH" bin/maskinen
ln -sf "$PRINT_PATH" bin/print
ln -sf "$POEM_PATH" bin/poem

chmod 755 scripts/*
export PATH="$PATH:$PWD/scripts"
